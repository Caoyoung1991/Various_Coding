// Name:
// loginid:
// CS 455 PA5

// pa5list.cpp
// a program to test the linked list code necessary for a hash table chain

// You are not required to submit this program for pa5.

// We gave you this starter file for it so you don't have to figure
// out the #include stuff.  The code that's being tested will be in
// listFuncs.cpp, which uses the header file listFuncs.h

// The pa5 Makefile includes a rule that compiles these two modules
// into one executable.
/*
#include <iostream>
#include <string>
#include <cassert>

using namespace std;

#include "listFuncs.h"


int main() {
	ListType list;
	listAddLast(list, new Node("lixiaomeng", 100));
	listAddLast(list, new Node("caoyang", 90));
	listAddLast(list, new Node("jim", 80));
	printList(list);
	if(listRemove(list, "lixiomeng") == false)
		cout << "error" << endl;
	listAddLast(list, new Node("caoyang", 1000));
	printList(list);
	if(listLookup(list, "cayang") != NULL)
		cout << "find!" << endl;
	else
		cout << "count find!" << endl;
	return 0;
}*/


